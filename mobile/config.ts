// Base URL of the Go server (cmd/server).
//
// Dev: your machine's LAN IP, reachable from the phone on the same Wi-Fi.
// Later (hosted server): swap this ONE line for e.g. "https://mtg.example.com"
// — nothing else in the app needs to change.
export const API_BASE_URL = "http://192.168.1.77:8090";
