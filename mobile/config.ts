// Base URL of the Go server (cmd/server). Change this one line for your setup;
// nothing else in the app depends on it.
//
//   - Testing on a physical phone (Expo Go): your dev machine's LAN IP on the
//     same Wi-Fi, e.g. "http://192.168.1.20:8090" (localhost won't work — that
//     resolves to the phone itself).
//   - iOS simulator / web: "http://localhost:8090".
//   - Hosted server: "https://your-server.example.com".
export const API_BASE_URL = "http://localhost:8090";
